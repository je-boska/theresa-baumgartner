---
import Layout from '../layouts/Layout.astro';
import { getPosts } from '../queries/Post';
import type { Post } from '../types/shared';
import Home from './Home';

const posts: Post[] = await getPosts();
---

<Layout title='Home'>
  <Home posts={posts} client:load />
</Layout>
